"use client";

import Logo from "@/components/logo";

export default function FormPage() {
  // This page is no longer in use and is pending removal.
  // The user is now directed to /dashboard after signing in.
  return (
    <main className="container mx-auto p-4 md:p-8">
      <div className="mb-8">
        <Logo />
      </div>
    </main>
  );
}
